"""Task-guided token scoring: score trajectory tokens by how much the worker
task/query attends to them.

TODO (Phase 4): implement task_guided_scores(attentions, traj_len, query_start, query_end).
"""
